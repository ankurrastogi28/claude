# Claude memory backup

Version-controlled backup of my Claude Code persistent memory for the `ph` workspace.

- `MEMORY.md` — the index loaded into context each session (one line per memory).
- `*.md` — individual memory files (one fact each, with frontmatter).

Synced from `~/.claude/projects/C--Users-arpan-repos-ph/memory/` on this machine.
