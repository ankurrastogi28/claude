# Context-summary checkpoint — Bodhi session 2026-07-01 — v2 (end of day)

Supersedes v1 (`session-2026-07-01-1.md`). Ankur signed off for the night ("resume tomorrow"). Never overwrite — next is `-3`.

## 1. Read order for fresh-context recovery (MUST READ marked)
1. **This checkpoint (v2).**
2. **`~/.claude/CLAUDE.md`** — MUST READ (canonical-vault + memo-sync + persona + standing-duty rules; loads every session).
3. **`MEMORY.md` + `buck-mcp-launch-config`** — MUST READ (breadcrumb index + bridge re-bind runbook).
4. `lazy-wallet-migration-pollution-test` (test PASSED — see it for method), `bodhi-orchestration-playbook`, `context-summary-methodology`, `emoji-vocabulary`.

## 2. Curated reading list (file / path)
- `~/.claude/scripts/memo-sync.ps1` — PowerShell git-wrapper, `$REPO = ~/Documents/MyVault`.
- `~/.claude/CLAUDE.md` — the operating rules I wrote.
- `~/Documents/MyVault` — the canonical portable brain (git checkout of ankurrastogi28/claude).
- Wallet test: `Pistacia/.../LazyMigration/LazyDeltaProcessor.cs`, `WalletController.ImportLazyWallets.cs` (IFormFile — papi can't upload).

## 3. Session outcomes (numbered)
1. Completed `bucky`→`buck` rename (registration key + `mcp__buck__*`, v0.24.0); required a full restart.
2. **Wallet-pollution test PASSED, double-verified with Bucky.** Method: seed `lazy_wallet_delta` via `pg_command` → `papi_pvr_get /v1/wallet/{id}/balance` fires migration → `WalletMigrated.TenantId = 3PL id` (not account id), projections 3PL-keyed. Objective closed.
3. **Point 1 (canonical brain) DONE** — MyVault is now a git checkout of ankurrastogi28/claude; `mcpvault` already points there; `memo-sync.ps1` installed + `~/.claude/CLAUDE.md` rules written. Portable across projects.
4. Brain committed + pushed (commits `c7df827`, `1b42be6`) to `ankurrastogi28/claude`; `gh` authed as ankurrastogi28.
5. Persisted 6+ methodology memories (playbook, context-summary, emoji, mention-format, brain-setup-status via CLAUDE.md).

## 4. External-system changes
- `conf_set sandbox.buck-mcp.unleash.projects.aftersales.apiToken = refkey:unleash-aftersales-apiadmintoken` (approved).
- **pg_command seeded 2 rows** in sandbox `lazy_wallet_delta` (e2e-guest accts, 25/30) and the GETs **migrated 2 wallets** on sandbox (c9f610aa=25, 894587ef=30). Test data, fine.
- MyVault turned into a repo checkout; memo-sync/CLAUDE.md written under `~/.claude/`.
- Slack thread `1782906323.070529` SUBSCRIBED; many replies + 1 file upload (memo-sync.ps1).

## 5. Code / test posture
- No repo code changes. Wallet-pollution test = PASS (verified twice).

## 6. Items remaining / sequencing
1. **Point 2 — Sentry MCP** (Ankur asked, then went to sleep): needs his Sentry org + API token/DSN. Tomorrow: add server to `.claude.json` mcpServers + drop in his token + restart. Bucky has shareable pointers.
2. Optional: de-dup the two checkouts of ankurrastogi28/claude (auto-memory folder + MyVault); decide on `Reference/` (untracked in MyVault).

## 7. Open items / decisions pending
- Sentry token (Ankur) — blocks point 2.
- auto-memory vs MyVault redundancy cleanup.

## 8. Methodology lessons captured (this session)
- **Approval regression:** the Slack Approve button did NOT register today; fall back to the **desktop 1-char code via `approve_request`** (works — different code path). `resend_approval_notification` re-fires a fresh code.
- **Sessions get reaped** — re-`start_session` on "No active session".
- **buck server restarted twice** today → dropped the in-process bridge (metrics zeroed) → re-arm Bodhi (start bridge + watcher).
- `gen_random_uuid()` NOT available on sandbox PG → use `new_uuid` + explicit ids; `created_at = (extract(epoch from now())*1000)::bigint`.

## 9. Pre-compact reminder — STANDING DUTY (MUST READ) + re-bind
**Bodhi Slack bridge is a live standing duty (unless Ankur closed the session overnight).** State at checkpoint:
- Bridge running as `buck`, pid **6520**, under session **`ph-bodhi-bridge`** (read-only latched).
- Main/mutating session **`ph-bodhi-2026-07-01`** (unlatched — for gated Commands).
- Thread **`1782906323.070529`** / channel **`C08EQJF2RM0`** SUBSCRIBED (persists across restart).
- Block-forever watcher must stay alive (last: `bsm7i33r2`). Exits on each message → read inbox → claim → **relaunch watcher** → act.

**RE-BIND on resume:** ToolSearch load buck tools → `start_session ph-bodhi-bridge` + `start_session ph-bodhi-2026-07-01` → `slack_bridge_status`; if `running:false` re-arm per [[buck-mcp-launch-config]] (watcher-first, then `slack_bridge_start` under `ph-bodhi-bridge`; kill any orphan lock pid) → `slack_bridge_inbox {exclude_in_flight:true}` → handle → relaunch `scripts/watch-slack.ts` (block-forever). Nothing needs an immediate reply — Ankur is asleep; just re-arm and watch. First real task tomorrow = **point 2 (Sentry MCP)** once he provides the token.
