# Context-summary checkpoint — Bodhi session 2026-07-01 — v1

First checkpoint for this session. Supersedes: none. (Never overwrite — next is `-2`.)
Written per [[context-summary-methodology]] during a long multi-thread Bodhi session.

## 1. Read order for fresh-context recovery (MUST READ marked)
1. **This checkpoint** (latest in `checkpoints/`).
2. **`MEMORY.md` + `buck-mcp-launch-config`** — MUST READ (my always-loaded memory + the standing-duty runbook).
3. **`lazy-wallet-migration-pollution-test`** memory — the paused primary task.
4. `bodhi-orchestration-playbook`, `context-summary-methodology`, `emoji-vocabulary`, `feedback-slack-mention-format` — adopted operating rules.

## 2. Curated reading list (file:line)
- `Pistacia/.../Wallet/LazyMigration/Import/WalletController.ImportLazyWallets.cs:32` — `/lazy-import` binds `IFormFile` (multipart) → papi tools can't upload it.
- `.../Import/LazyCsvRow.cs` — CSV cols `three_pl_id,customer_email,legacy_balance`.
- `.../LazyMigration/LazyDeltaProcessor.cs` — GET-triggered lazy delta → wallet events (the pollution-critical path).
- `C:\Users\arpan\tmp\buck-mcp\lazy-wallet-migration-test.csv` — the staged 4-row test CSV.
- `C:\Users\arpan\tmp\buck-mcp\memo-sync.ps1` — the PowerShell port (draft).

## 3. Session outcomes (numbered)
1. Completed the `bucky`→`buck` rename: renamed `.claude.json` top-level `mcpServers` key `bucky`→`buck`; tools now `mcp__buck__*`; buck-mcp v0.24.0. Required a full Claude Code restart (soft `/mcp` reconnect was insufficient).
2. Re-armed Bodhi under `buck` after restart.
3. Wired Acacia into buck-mcp via `proconf_populate` (node `Acacia.Host.EventStore`); sourced Cohort B onboarded accounts from `$ce-Acacia_Account`.
4. Verified the wallet flag firsthand: Unleash key `pistacia_enable_lazy_wallet_migration` = enabled dev+prod (via `aftersales` project + admin token).
5. Discovered Ankur already has an Obsidian vault MCP: `mcpvault` → `C:\Users\arpan\Documents\MyVault`.
6. Ported `memo-sync` bash → `memo-sync.ps1` (uploaded to Slack).
7. Persisted 6 memory notes (playbook, context-summary, emoji, mention-format, lazy-wallet update, launch-config update).

## 4. External-system changes
- 2× approved `conf_set` on `sandbox.buck-mcp.unleash.projects.aftersales.apiToken` — ended on `refkey:unleash-aftersales-apiadmintoken` (admin token; the client `apitoken` 403s on the Admin API).
- Slack: subscribed thread `1782906323.070529`; many replies; uploaded `memo-sync.ps1` (F0BEBK2U083). Downloaded 2 team docs to `tmp/buck-mcp/slack/files/`.
- Memory dir: 3 new notes + MEMORY.md index + edits to launch-config & lazy-wallet notes.

## 5. Code / test posture
- No code changes to any repo. The `/lazy-import` wallet-pollution test has NOT run yet.

## 6. Items remaining / sequencing
1. Run the lazy-import pollution test once Ankur picks **(a)** curl to the real endpoint or **(b)** seed `lazy_wallet_delta` via `pg_command` + drive GETs with `papi_pvr_get` (recommended b). Then verify: ESDB `$ce-Pistacia_Wallet` TenantId = 3PL id (61d0547f…), PG `wallet_balance`/`wallet_ledger` 3PL-keyed, deltas `processed=true`. Bucky (U0AT6JCGXKK) on standby to co-verify.
2. On Ankur's go: inspect `MyVault`, git-back + populate, wire `CLAUDE.md` canonical-vault rule + `memo-sync` hook, add Sentry MCP.

## 7. Open items / decisions pending
- Lazy-import approach a/b — WAITING ON OWNER.
- Vault setup go + Sentry MCP add — WAITING ON OWNER.

## 8. Methodology lessons captured (persisted to memory)
- Slack mentions use `<@ID>` not `@ID`. Emoji vocab (circles/squares/pictograms) for CLI. Watcher wraps the bridge; claim→relaunch-before-processing→act. Context-summary = versioned checkpoint, never overwrite, breadcrumb standing duties.

## 9. Pre-compact reminder — STANDING DUTY (MUST READ) + re-bind
**Bodhi Slack bridge is a live standing duty.** Current live state:
- Bridge running as `buck`, pid 7628, started 14:49, under bridge session **`ph-bodhi-bridge`** (read-only latched).
- Mutating/main session **`ph-bodhi-2026-07-01`** (unlatched — use for gated Commands like conf_set/pg_command). NOTE: sessions get reaped; re-`start_session` if "no active session".
- Thread **`1782906323.070529`** in channel **`C08EQJF2RM0`** is SUBSCRIBED (all messages ingest).
- A block-forever watcher must always be running (last: `bykk978yz`). It exits on each message → I read inbox → claim → **relaunch watcher** → act.

**RE-BIND ACTION on resume:** ToolSearch `select:mcp__buck__start_session,...` → `start_session ph-bodhi-bridge` + `start_session ph-bodhi-2026-07-01` → `slack_bridge_status` (if not running, re-arm per [[buck-mcp-launch-config]]: watcher-first then `slack_bridge_start` with a separate session_id; clear any orphan pid holding the lock) → `slack_bridge_inbox {exclude_in_flight:true}` → handle → relaunch `scripts/watch-slack.ts` (block-forever, no --timeout). Start here: no message needs an immediate reply; just re-arm and resume watching.
